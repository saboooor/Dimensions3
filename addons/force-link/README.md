# Force Link Addon

An administrative utility addon for the **Dimensions** plugin that allows administrators to manually link two portals together.

## Command Usage
- `/dim forceLink select` (while looking at a portal) - Selects the first portal.
- `/dim forceLink set` (while looking at a second portal) - Links the second portal with the selected one.
